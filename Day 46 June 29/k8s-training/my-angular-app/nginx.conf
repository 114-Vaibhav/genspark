server {
    listen 80;
    server_name localhost;

    # The directory where your Angular build files are located
    root /usr/share/nginx/html;
    index index.html;

    # This routes all requests to index.html so Angular's router can handle them
    location / {
        try_files $uri $uri/ /index.html;
    }
}